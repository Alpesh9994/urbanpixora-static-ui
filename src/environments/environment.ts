export const environment = {
    production: false,
    // Ensure the user replaces these with their actual EmailJS credentials
    emailjs: {
        serviceId: 'YOUR_SERVICE_ID',
        templateId: 'YOUR_TEMPLATE_ID',
        publicKey: 'YOUR_PUBLIC_KEY'
    }
};
